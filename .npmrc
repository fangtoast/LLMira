# Purpose: Keep local and CI dependency resolution on the canonical npm registry.
# Author: fangtoast <fangtoast@foxmail.com>
registry=https://registry.npmjs.org/
replace-registry-host=always
engine-strict=true
